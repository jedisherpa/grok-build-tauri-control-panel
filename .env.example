# Optional: Grok Build / xAI credentials for live ACP sessions
XAI_API_KEY=

# Optional: override binary path (else discovered via PATH / ~/.cargo/bin)
# GROK_BINARY=/path/to/grok

# Logging
# RUST_LOG=info,grok_acp=debug,grok_control_core=debug
